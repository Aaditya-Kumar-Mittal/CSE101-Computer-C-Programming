#include <stdio.h>
int main()
{
    int a = 50;
    if (a > 50);
        //shows that we can implement if statement after ; but not if we have a else statement. This time it will show error
    printf("Hello");
    printf("\nHi");
    return 0;
}