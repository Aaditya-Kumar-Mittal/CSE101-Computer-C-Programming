#include <stdio.h>
int main()
{
    int j, i, n;
    printf("Enter the count value: ");
    scanf("%d", &n);

    for (i = 0; (j = 0), i < n; i++)
    {
        printf("%d %d\n", i, j);
    }

    return 0;
}
